{
  "manifest_version": "0.3",
  "name": "mason",
  "display_name": "Mason",
  "version": "0.0.0",
  "description": "Context engineering MCP server — concept maps for codebases so LLMs stop re-exploring every session.",
  "long_description": "Mason persists a feature-to-file concept map in `.mason/snapshot.json` so AI assistants can answer architecture questions without grepping the codebase from scratch. Also exposes change impact analysis (co-change history, references, related tests) and git history aggregation (hot files, stale dirs, commit patterns).",
  "author": {
    "name": "Adrian Czuczka",
    "url": "https://github.com/adrianczuczka"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/adrianczuczka/mason"
  },
  "homepage": "https://github.com/adrianczuczka/mason",
  "support": "https://github.com/adrianczuczka/mason/issues",
  "license": "MIT",
  "keywords": [
    "context-engineering",
    "claude",
    "claude-md",
    "mcp",
    "code-analysis",
    "snapshot",
    "concept-map"
  ],
  "server": {
    "type": "node",
    "entry_point": "dist/bin/mason-mcp.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/dist/bin/mason-mcp.js"]
    }
  },
  "tools": [
    {
      "name": "get_snapshot",
      "description": "Load the concept map — a lookup table from features and flows to the files that implement them. Use this to jump straight to relevant files instead of exploring.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "dir": { "type": "string", "description": "Absolute path to the project root directory" }
        },
        "required": ["dir"]
      }
    },
    {
      "name": "save_snapshot",
      "description": "Save a concept-to-files map as a persistent project snapshot. Persists across conversations.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "dir": { "type": "string", "description": "Absolute path to the project root directory" },
          "features": {
            "type": "object",
            "description": "Map of feature names to their implementing files",
            "additionalProperties": {
              "type": "object",
              "properties": {
                "description": { "type": "string" },
                "files": { "type": "array", "items": { "type": "string" } },
                "tests": { "type": "array", "items": { "type": "string" } }
              },
              "required": ["description", "files"]
            }
          },
          "flows": {
            "type": "object",
            "description": "Map of flow names to ordered file chains",
            "additionalProperties": {
              "type": "object",
              "properties": {
                "description": { "type": "string" },
                "chain": { "type": "array", "items": { "type": "string" } }
              },
              "required": ["description", "chain"]
            }
          }
        },
        "required": ["dir", "features", "flows"]
      }
    },
    {
      "name": "get_impact",
      "description": "Change impact analysis. Returns git co-change history, references, and related tests for the given files. Use before editing a file to understand what else might need updating.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "dir": { "type": "string", "description": "Absolute path to the project root directory" },
          "files": {
            "type": "array",
            "items": { "type": "string" },
            "description": "File paths or names to analyze"
          }
        },
        "required": ["dir", "files"]
      }
    },
    {
      "name": "analyze_project",
      "description": "Git history analysis: commit conventions, stale directories, frequently changed (hot) files.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "dir": { "type": "string", "description": "Absolute path to the project root directory" }
        },
        "required": ["dir"]
      }
    },
    {
      "name": "full_analysis",
      "description": "Run a complete project analysis in one call: git stats, project structure, curated code samples, and test-to-source mapping. Recommended starting point on a new codebase.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "dir": { "type": "string", "description": "Absolute path to the project root directory" }
        },
        "required": ["dir"]
      }
    },
    {
      "name": "get_code_samples",
      "description": "Get previews (~60 lines) of representative source files: entry points, config, hot files, test examples, and one file per directory.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "dir": { "type": "string", "description": "Absolute path to the project root directory" },
          "count": { "type": "number", "description": "Maximum number of files to sample", "default": 15 }
        },
        "required": ["dir"]
      }
    }
  ],
  "compatibility": {
    "platforms": ["darwin", "linux", "win32"],
    "runtimes": {
      "node": ">=20.0.0"
    }
  }
}
