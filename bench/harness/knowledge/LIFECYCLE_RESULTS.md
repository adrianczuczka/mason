# R8 lifecycle development smoke — 2026-09-11

One live Codex trial completed after building the [conditional lifecycle case](LIFECYCLE.md). Both approaches captured useful knowledge and produced working release patches. Ordinary notes completed the revision and final reuse path; the Mason session added a second proposal instead of revising the existing accepted record.

| Outcome | Mason with current guidance | Explicit project notes |
|---|---|---|
| Capture a sourced, conditional lesson without naming Mason in the task | Passed independent review | Passed independent review |
| Remove the unused integration and preserve release startup | Passed | Passed |
| Unrelated greeting edit produces no knowledge record | Passed | Passed |
| Required background integration preserves release startup | Passed; targeted member keep rule | Passed; targeted class/constructor keep rule |
| Revise the existing lesson when its premise changes | Not completed; created a second proposal | Passed independent review |
| Further fresh-session reuse after revision review | Not run after the missing revision | Passed; added cleanup without changing knowledge |

Mason retrieved the accepted original before editing. The new proposal explained the changed requirement, retained the reflection hazard, and correctly left the original accepted revision operative. It did not silently approve its own work. The missing outcome was maintaining the existing record through a reviewed revision. A separately reviewed replacement and retirement could also be a legitimate maintenance workflow; this trial did not demonstrate it.

The notes revision explicitly replaced the foreground-only removal recommendation. It retained the monitoring lesson as inherited accepted evidence, disclosed that the original incident file was absent, and limited its new verification claim to the JVM model. The final session read the reviewed notes, preserved the existing forecast job, and added cleanup with release startup still passing.

## Scope and reproducibility

- Seven actual sessions, fresh conversations and checkouts, no model retries or seeded captures. Codex CLI `0.153.4`; host-default model selection, with model identity unreported by the runner. Hooks and native private memory disabled.
- R8 `8.10.9-dev`, build `a7ad18a70460b799d0482e497c109a75bf7f91de`; `javac 17.0.17`. R8 artifact SHA-256: `4097ff9c46c185c6e7214da7fe9b1befb5adeea5cc9ca349270e0249904f9240`.
- Preflight reproduced the debug/release difference and constructor-lookup failure, dependency-removal success, and two valid keep-rule repairs. The focused harness regressions passed, including real MCP observation, two reviews through final commits, stale-review rejection, changed-checker rejection, and unrelated-task controls.
- Independent proposal assessments were performed by the implementing Codex agent, separately from the evaluated sessions. They were **not blind** and apply only to the reconstructed fixture.
- Two grading defects were corrected using the same retained artifacts: an exact `Mobile team` owner requirement rejected the equivalent `Mobile` label, and a hidden `forecast` job identifier rejected other valid labels. Original reports and grader identities were archived; proposals, patches and session transcripts were not rewritten or rerun. Applied reviews were preserved.
- The ignored local run is `lifecycle-live-codex-1` under `.mason/reports/`. Its report retains all outcomes, regrading history, review receipts, transcripts, tool calls, per-session latency and usage. Dollar costs remain unknown; input usage includes reported cached input.

This is a known development incident reconstructed as a small JVM model. It does not run Android, Room, WorkManager, device startup or real scheduling. It does not measure a population false-positive rate or prove a causal benefit from retrieved context. The notes guidance explicitly asks for revision when conditions change; Mason uses its current product guidance. That difference is part of the workflow comparison, not a controlled comparison of storage mechanisms alone.

## Next experiment

The clearer revision guidance has now been implemented. The [unchanged repeat and interrupted-upload probe](REVISION_GUIDANCE_RESULTS.md) record the subsequent results without replacing this original miss. Preserve cases where a new record is appropriate; do not impose a blanket one-record rule. Independent ElevenLabs validation and broader trials remain later work.
